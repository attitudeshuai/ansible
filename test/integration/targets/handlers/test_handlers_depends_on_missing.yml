---
- hosts: localhost
  gather_facts: false
  tasks:
    - set_fact:
        dep: 1
  handlers:
    - name: existing_handler
      set_fact:
        dep: 2
      depends_on: handler_that_does_not_exist
